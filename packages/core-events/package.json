{
  "name": "@gx/core-events",
  "version": "1.0.0",
  "private": true,
  "description": "Event schema definitions and validation for Fabric events",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "jest",
    "generate-types": "node scripts/generate-types.js"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^13.1.1"
  }
}
